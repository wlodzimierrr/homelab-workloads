apiVersion: v1
kind: Service
metadata:
  name: scaffold-smoke
  namespace: scaffold-smoke
  labels:
    app.kubernetes.io/name: scaffold-smoke
    app.kubernetes.io/instance: scaffold-smoke
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: scaffold-smoke
  ports:
    - name: http
      port: 80
      targetPort: http
