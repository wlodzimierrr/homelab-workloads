# Copy to a secure local file (do not commit) and use with:
# kubectl -n homelab-api create secret generic homelab-api-postgres \
#   --from-env-file=<your-secure-file> --dry-run=client -o yaml | kubectl apply -f -
POSTGRES_DB=homelab
POSTGRES_USER=homelab
POSTGRES_PASSWORD=replace-with-strong-password
