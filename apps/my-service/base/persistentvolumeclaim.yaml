apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-service-wp-content
  namespace: my-service
  labels:
    app.kubernetes.io/name: my-service
    app.kubernetes.io/component: web
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
