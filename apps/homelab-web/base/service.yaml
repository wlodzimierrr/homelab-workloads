apiVersion: v1
kind: Service
metadata:
  name: homelab-web
  namespace: homelab-web
  labels:
    app.kubernetes.io/name: homelab-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: homelab-web
  ports:
    - name: http
      port: 80
      targetPort: http
