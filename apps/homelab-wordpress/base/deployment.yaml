apiVersion: apps/v1
kind: Deployment
metadata:
  name: homelab-wordpress
  namespace: homelab-wordpress
  labels:
    app.kubernetes.io/name: homelab-wordpress
    app.kubernetes.io/instance: homelab-wordpress
    app.kubernetes.io/component: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: homelab-wordpress
      app.kubernetes.io/component: web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: homelab-wordpress
        app.kubernetes.io/component: web
    spec:
      serviceAccountName: homelab-wordpress
      containers:
        - name: web
          image: wordpress:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          env:
            - name: WORDPRESS_DB_HOST
              value: homelab-wordpress-mysql:3306
            - name: WORDPRESS_DB_USER
              valueFrom:
                secretKeyRef:
                  name: homelab-wordpress-wordpress-db
                  key: WORDPRESS_DB_USER
            - name: WORDPRESS_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: homelab-wordpress-wordpress-db
                  key: WORDPRESS_DB_PASSWORD
            - name: WORDPRESS_DB_NAME
              valueFrom:
                secretKeyRef:
                  name: homelab-wordpress-wordpress-db
                  key: WORDPRESS_DB_NAME
          readinessProbe:
            httpGet:
              path: /wp-login.php
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /wp-login.php
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          volumeMounts:
            - name: wp-content
              mountPath: /var/www/html/wp-content
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: wp-content
          persistentVolumeClaim:
            claimName: homelab-wordpress-wp-content
